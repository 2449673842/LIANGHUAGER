# 项目进度

更新时间：2026-05-17

## 已完成

- 独立目录 `E:\codex版本实盘回测` 已作为主线项目。
- 前端完成 Vue 3 工作台壳层和五个一级页面。
- 后端完成 FastAPI 路由骨架和数据兜底机制。
- K 线数据按 `OKX CLI -> OKX REST -> quant_platform -> fallback` 的顺序读取。
- 行情快照和盘口已接入 OKX CLI/REST。
- 总览页支持自选交易对、监控开关、同步数据和自动刷新。
- 模拟/实盘页支持账户添加、执行队列添加和待确认任务查看。
- 回测页支持周期选择和运行回测入口。
- 策略中心支持策略导入。
- 通用 `ModalDialog` 已替换简单 prompt。
- 前端构建通过：`npm run build`。
- 后端主要模块语法检查通过。

## 刚修复

- `frontend/src/services/api.ts` 补齐 `ApiEnvelope<T>` 类型，解决升级后前端构建失败问题。

## 当前限制

- 账户、队列、自选和策略导入暂时是内存态，重启后不保留。
- 回测运行结果仍是本地模拟，尚未接入旧项目真实回测引擎。
- 实盘下单、账户资产、持仓和人工确认执行还没有封装成安全可用的后端端点。
- 通知链路预留了邮件和企业微信方向，但尚未完成配置页和发送接口。
- 前端构建通过，但 ECharts 相关 bundle 偏大，后续需要拆分。

## 建议下一步

1. 持久化自选、账户、队列和策略导入状态。
2. 把旧 `quant_platform` 的真实回测运行接口接到 `/api/backtests/run`。
3. 新增 OKX 账户资产、持仓、订单确认和执行确认端点。
4. 把执行页从展示型队列升级为真实确认流。
5. 在设置页补齐 OKX CLI 路径、上游地址、SMTP、企业微信配置。
6. 对前端 ECharts 做按页懒加载或 manual chunk 拆包。
